# UML Exports

Export the four required diagrams into this directory using these base names:

```text
use-case.png
class-diagram.png
activity-diagram.png
state-machine.png
```

PDF is also accepted for any diagram. The strict package command requires one PNG or PDF for every base name. Export files are ignored by Git and copied into the final submission ZIP.
