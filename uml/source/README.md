# Editable UML Source

Place the final editable Visual Paradigm file here as:

```text
GuideMyPC.vpp
```

The file is ignored by Git and copied into the generated submission ZIP by `composer run package:submission:strict`.
